class DisplayModel:
    def __init__(self, message, speed, font, background):
        # super().__init__()
        self.message = message
        self.speed = speed
        self.font = font
        self.background = background